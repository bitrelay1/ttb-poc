[build]
builder = "nixpacks"
buildCommand = "cd frontend && npm ci && npm run build && cd ../backend && pip install ."

[deploy]
startCommand = "cd backend && alembic upgrade head && uvicorn app.main:app --host 0.0.0.0 --port $PORT"
restartPolicyType = "on_failure"
